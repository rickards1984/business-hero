# Backend dependency lock — generated, do not hand-edit.
#
# WHY: root requirements.txt pins nothing (19 requirement lines, 0 pins), so
# every clean install — CI and every Railway deploy — resolved to whatever was
# newest that day. Review 001 finding 3 called this out: matching ruff and
# pytest alone does not make two environments equivalent.
#
# HOW IT IS USED: as pip CONSTRAINTS, not as the install list —
#
#     pip install -r requirements.txt -c requirements.lock.txt
#
# so the resolution still comes from requirements.txt (the same file Railway
# installs, per AGENTS.md §3.1) while every version is pinned. Installing the
# lock directly would test a different input from the one production uses.
#
# REGENERATE after changing requirements.txt:
#
#     python3.12 -m venv /tmp/lockenv
#     /tmp/lockenv/bin/python -m pip install -r requirements.txt
#     /tmp/lockenv/bin/python -m pip freeze > requirements.lock.txt
#     # then restore this header
#
# A dependency added to requirements.txt but not present here installs
# UNPINNED — constraints only pin what they list. Regenerate, do not append.
#
# EVIDENCE for this lock: generated 2026-09-08 in an empty venv on Python
# 3.12.14 from requirements.txt alone. `pip check` reported no broken
# requirements; `ruff check backend` passed; `pytest backend/tests` reported
# 401 passed, 104 subtests passed. Captured in
# audits/foundation-review-002-clean-install.txt.
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiohttp-retry==2.9.1
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1
Deprecated==1.3.1
et_xmlfile==2.0.0
fastapi==0.141.1
frozenlist==1.8.0
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
jiter==0.16.0
limits==5.8.0
multidict==6.7.1
openai==3.9.0
openpyxl==3.1.5
packaging==26.3
pillow==12.3.0
propcache==0.5.2
psycopg2-binary==2.9.12
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
PyJWT==2.13.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
python-multipart==0.0.32
pytz==2026.3.post1
PyYAML==6.0.3
reportlab==5.0.1
requests==2.34.2
six==1.17.0
slowapi==0.1.10
sniffio==1.3.1
SQLAlchemy==2.0.52
sqlmodel==0.0.42
starlette==1.6.0
stripe==15.6.1
truststore==0.10.4
twilio==9.11.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
wrapt==2.4.0
yarl==1.24.5
